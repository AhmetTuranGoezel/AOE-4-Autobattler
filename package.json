{
  "name": "owl-tools",
  "private": true,
  "description": "Static tools site. The only dependency is the storage client used by the Netlify sync function; on Cloudflare Pages nothing here is needed.",
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  }
}
