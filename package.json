{
  "name": "owl-tools",
  "private": true,
  "description": "Static tools site. The single dependency is the storage client for the Netlify function behind the Pokemon Champions sync.",
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  }
}
