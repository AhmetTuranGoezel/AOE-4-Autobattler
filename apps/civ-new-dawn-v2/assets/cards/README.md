Card art sheets live here, fetched by `../../tools/fetch-card-art.sh`.

They are deliberately not committed: they are scans of published Fantasy Flight /
Asmodee artwork. `card-manifest.json` records only *where each card sits* in each
sheet, so the layout survives without shipping the images.

With the sheets missing the app renders exactly as before — the art is additive.
